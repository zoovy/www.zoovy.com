#!/usr/bin/perl

use lib "/httpd/modules";
use ZOOVY;

if ($ENV{'REQUEST_METHOD'} eq 'POST') {
	$/ = undef; $xml = <STDIN>; $/ = "\n";
	$xml =~ s/[\r]+//gs;
	}

 


print "Content-type: text/xml\n\n";
